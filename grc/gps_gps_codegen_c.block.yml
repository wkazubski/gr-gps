# auto-generated by grc.converter

id: gps_gps_codegen_c
label: gps_codegen_c
category: gps

parameters:
-   id: samplerate
    label: Sample rate
    dtype: float
-   id: code
    label: Code selection
    dtype: int
-   id: datamode
    label: Data generation mode
    dtype: int

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gps
    make: gps.gps_codegen_c(${samplerate}, ${code}, ${datamode})
    callbacks:
    - set_code(int(${code}))
    - set_datamode(int(${code}))
    - set_sample_rate(${samplerate})

file_format: 1
