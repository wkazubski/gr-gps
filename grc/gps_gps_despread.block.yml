# auto-generated by grc.converter

id: gps_gps_despread
label: gps_despread
category: gps

parameters:
-   id: code_sel
    label: Spreading code
    dtype: int
-   id: osr_sel
    label: Oversampling
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gps
    make: gps.gps_despread(${code_sel}, ${osr_sel})
    callbacks:
    - set_code(int(${code_sel}))

file_format: 1
